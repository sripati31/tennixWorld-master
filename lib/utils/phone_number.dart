import 'package:libphonenumber/libphonenumber.dart';

Future<bool> isValidPhoneNumber({
  required String phoneNumber,
}) async {
  // bool _isValid = (await PhoneNumberUtil.isValidPhoneNumber(phoneNumber: phoneNumber.trim(), isoCode: 'IN'))??false;
  return true;
}
